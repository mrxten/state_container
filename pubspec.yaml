name: state_container
description: Flutter widget for control different states.
version: 0.0.5
homepage: https://github.com/mrxten/state_container

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  animations: ^1.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter: